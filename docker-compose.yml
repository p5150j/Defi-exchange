version: '2'
services:
  db:
    image: mysql:5.7
    environment:
      MYSQL_ROOT_PASSWORD: HrAgMJ&Jc37^zMr(
    env_file: .env.docker
    ports:
      - "3306:3306"
    volumes:
      - ./volumes/mysql:/var/lib/mysql

  backend:
    build:
      context: backend
    volumes:
      - "./backend/src:/app/src"
      - /app/node_modules
    ports:
      - "3333:3333"
    depends_on:
      - db
    env_file: .env.docker
    command: yarn dev
