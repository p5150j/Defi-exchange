{
  "name": "backend",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "main": "index.ts",
  "scripts": {
    "preinstall": "use-yarn &>/dev/null || (npm install -g use-yarn && use-yarn)",
    "start": "node build",
    "dev": "nodemon",
    "prebuild": "rm -rf build",
    "build": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "@types/node": "^9.4.7",
    "@types/restify": "^5.0.7",
    "bunyan-prettystream-circularsafe": "^0.3.1",
    "mysql2": "^1.5.2",
    "reflect-metadata": "^0.1.12",
    "restify": "^6.3.4",
    "restify-cors-middleware": "^1.1.0",
    "sequelize": "^4.36.1",
    "sequelize-typescript": "^0.6.3",
    "ts-node": "^5.0.1",
    "typescript": "^2.7.2"
  },
  "devDependencies": {
    "nodemon": "^1.17.1"
  }
}
